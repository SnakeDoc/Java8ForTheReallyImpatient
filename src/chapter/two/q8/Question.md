Q: Write a method `public static <T> Stream<T> zip(Stream<T> first, Stream(T> second)` that alternates elements from the streams `first` and `second`, stopping when one of them runs out of elements.

A: 
